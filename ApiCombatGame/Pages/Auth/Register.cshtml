@page
@model ApiCombatGame.Pages.Auth.RegisterModel
@{
    ViewData["Title"] = "Sign Up";
}

<div class="max-w-md mx-auto px-4">
    <div class="md3-card p-8">
        <h1 class="text-2xl font-bold text-center mb-6" style="color: var(--md-sys-color-on-surface);">Create Account</h1>

        @if (!string.IsNullOrEmpty(Model.ErrorMessage))
        {
            <div class="md3-alert md3-alert-error mb-6">
                <p class="text-sm">@Model.ErrorMessage</p>
            </div>
        }

        <form method="post">
            <div class="mb-4">
                <md-outlined-text-field
                    label="Username"
                    name="Input.Username"
                    id="Input_Username"
                    required
                    value="@Model.Input?.Username"
                    placeholder="Choose a username"
                    class="w-full">
                </md-outlined-text-field>
                <span asp-validation-for="Input.Username" class="field-validation-error"></span>
            </div>

            <div class="mb-4">
                <md-outlined-text-field
                    label="Email"
                    type="email"
                    name="Input.Email"
                    id="Input_Email"
                    required
                    value="@Model.Input?.Email"
                    placeholder="you@@example.com"
                    class="w-full">
                </md-outlined-text-field>
                <span asp-validation-for="Input.Email" class="field-validation-error"></span>
            </div>

            <div class="mb-4">
                <md-outlined-text-field
                    label="Password"
                    type="password"
                    name="Input.Password"
                    id="Input_Password"
                    required
                    placeholder="Minimum 8 characters"
                    class="w-full">
                </md-outlined-text-field>
                <span asp-validation-for="Input.Password" class="field-validation-error"></span>
            </div>

            <div class="mb-6">
                <md-outlined-text-field
                    label="Confirm Password"
                    type="password"
                    name="Input.ConfirmPassword"
                    id="Input_ConfirmPassword"
                    required
                    placeholder="Repeat your password"
                    class="w-full">
                </md-outlined-text-field>
                <span asp-validation-for="Input.ConfirmPassword" class="field-validation-error"></span>
            </div>

            <md-filled-button type="submit" class="w-full">
                Create Account
            </md-filled-button>
        </form>

        <p class="text-center text-sm mt-6" style="color: var(--md-sys-color-on-surface-variant);">
            Already have an account?
            <a asp-page="/Auth/Login" class="font-medium" style="color: var(--md-sys-color-primary);">Login</a>
        </p>

        <p class="text-center text-xs mt-4" style="color: var(--md-sys-color-outline);">
            By creating an account, you agree to our
            <a asp-page="/Privacy" class="underline" style="color: var(--md-sys-color-outline);">Privacy Policy</a>.
        </p>
    </div>
</div>
